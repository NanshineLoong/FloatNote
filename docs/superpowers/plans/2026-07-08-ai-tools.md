# AI Tools 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 sidecar 暴露给 tutor 的工具从 2 个（read_note/write_note）扩到 7 个（加 edit_note、set_tag、tag_create、tag_delete、list_tags），写操作经"凡写必确认"气泡放行，并按目标文件类型决定是否可选"保存快照后写入"。

**Architecture:** 纯 TS 逻辑抽到共享包 `@floatnote/note-logic`，前端与 sidecar 共用；sidecar 工具层 pull 当前全文、跑共享逻辑算出新全文、把"工具名+旧全文+新全文+preview"发给 Rust；Rust 作为唯一写入方，先弹 `permission://request` 气泡，用户允许后做 `old_content` 并发校验、按 `write_mode` 决定是否快照、落盘、广播 `note://updated`、回结果。

**Tech Stack:** TypeScript（Vite 前端 + tsx sidecar）、Rust/Tauri 2、CodeMirror 6、Vitest、Pi SDK（`@earendil-works/pi-coding-agent`）。

## Global Constraints

- TypeScript ES modules，两空格缩进、双引号、分号、camelCase。Rust 用 rustfmt、snake_case。
- 共享逻辑放 `shared/note-logic/`，前端与 sidecar 都通过包名 `@floatnote/note-logic` import，禁止两份实现。
- 写操作**只在 Rust 落盘**；sidecar 不碰文件系统。
- 版本快照**仅对 `kind === "piece"` 生效**；`_inbox`/`_tasks`/散文档不快照（散文档即便 note_id 不带 `_` 也不在气泡给快照选项，Rust 后端再兜底）。
- `target` 语义：`{ kind: "inbox" | "tasks" | "piece" | "doc"; name?: string }`，缺省=当前活动笔记。
- 每个任务结束 `npm test`（前端/共享）或 `cargo check`/sidecar `npm test` 通过后提交。
- 提交信息短祈使句、带 conventional 前缀，结尾加 `Co-Authored-By: Claude <noreply@anthropic.com>`。

---

## 文件结构

- `shared/note-logic/` — 新建本地 workspace 包。
  - `src/index.ts` — re-export。
  - `src/blocks/ranges.ts`、`src/tags/model.ts`、`src/tasks.ts` — 从 `src/note/` 搬迁，纯逻辑。
  - `src/matching.ts` — 新增：anchor 块定位 + str_replace 唯一性。
  - `src/*.test.ts` — 随迁测试 + 新测试。
  - `package.json`、`tsconfig.json`。
- `package.json`（根）— 加 `workspaces` 与 `@floatnote/note-logic` 依赖。
- `sidecar/package.json` — 加 `@floatnote/note-logic: "file:../shared/note-logic"` 依赖。
- `sidecar/src/protocol.ts` — 加 `ApplyEdit`/`ApplyEditResult`/`GetNoteText`/`NoteText`，废弃 `apply_write`/`apply_write_result`。
- `sidecar/src/note-tools.ts` — 重写为 7 工具，deps 扩 `getNoteText`/`requestWrite`（带 preview/target）。
- `sidecar/src/agent.ts` — `AgentRunner` 加 `getNoteText` 往返、`requestWrite` 发 `ApplyEdit`、处理 `denied`；`createAgentSession` 的 `tools` 列表更新。
- `sidecar/src/main.ts` — 分派新 `apply_edit_result`/`note_text`，移除旧 `apply_write_result`。
- `sidecar/src/tutor-prompt.ts` — 更新工具说明。
- `src-tauri/src/agent.rs` — `HostToSidecar`/`SidecarToHost` 加变体；`handle_apply_write`→`handle_apply_edit`（target 解析、并发校验、write_mode、permission 往返）；`handle_get_note_text`；`permission://request` emit + `resolve_permission` 命令入口。
- `src-tauri/src/commands.rs` — 注册 `resolve_permission`、`get_note_text` 命令（若走命令通道）。
- `src/note/main.ts` — 前端订阅 `permission://request`，挂气泡；import 改包名。
- `src/note/editor.ts` — 无需改（已可撤销），仅加回归测试。
- `src/assistant/permission-bubble.ts` — 新建：气泡组件。
- `src/assistant/assistant.ts` — 挂载气泡 region。

---

### Task 1: 抽 `@floatnote/note-logic` 共享包

把纯逻辑从前端搬到共享包，前端改 import，不改变行为。先建包与 workspace，再搬文件，最后改前端 import 并跑测试。

**Files:**
- Create: `shared/note-logic/package.json`
- Create: `shared/note-logic/tsconfig.json`
- Create: `shared/note-logic/src/index.ts`
- Move: `src/note/blocks/ranges.ts` → `shared/note-logic/src/blocks/ranges.ts`
- Move: `src/note/tags/model.ts` → `shared/note-logic/src/tags/model.ts`
- Move: `src/note/tasks.ts` → `shared/note-logic/src/tasks.ts`
- Move: 对应 `*.test.ts` 一并搬迁
- Modify: `package.json`（根）
- Modify: `sidecar/package.json`
- Modify: `src/note/` 下所有 import 这些模块的文件

**Interfaces:**
- Produces: npm 包 `@floatnote/note-logic`，`exports` 指向 `src/index.ts`（TS 源），导出 `BlockRange`/`ChangeOp`/`blockRanges`、`TagDef`/`TagMap`/`parseDefs`/`serializeDefs`/`setBlockTagChange`/`addTagDefChange`/`deleteTagChanges`/`blockTagId`/`stripTagMarker`/`isTagColorTaken`/`uniqueSlug`、`TaskLine`/`parseTasks`/`serializeTasks`/`toggleTask`/`addTask`/`deleteTask`。

- [ ] **Step 1: 建包骨架**

Create `shared/note-logic/package.json`:
```json
{
  "name": "@floatnote/note-logic",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  }
}
```

Create `shared/note-logic/tsconfig.json`:
```json
{
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "target": "ES2021",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "types": ["vitest/globals"]
  },
  "include": ["src"]
}
```

- [ ] **Step 2: 搬迁源文件**

`git mv src/note/blocks/ranges.ts shared/note-logic/src/blocks/ranges.ts`，同样搬 `ranges.test.ts`、`tags/model.ts`、`tags/model.test.ts`、`tasks.ts`、`tasks.test.ts`。搬迁后修正内部相对 import：`tags/model.ts` 里 `from "../blocks/ranges"` 改为 `from "../blocks/ranges"`（同包内路径不变，无需改）。

Create `shared/note-logic/src/index.ts`:
```typescript
export * from "./blocks/ranges.js";
export * from "./tags/model.js";
export * from "./tasks.js";
```

- [ ] **Step 3: 注册 workspace 与依赖**

根 `package.json` 加：
```json
  "workspaces": ["shared/note-logic"],
```
并在 `dependencies` 加 `"@floatnote/note-logic": "*"`。

`sidecar/package.json` 的 `dependencies` 加：
```json
    "@floatnote/note-logic": "file:../shared/note-logic"
```

Run: `npm install`（根）与 `cd sidecar && npm install`。

- [ ] **Step 4: 改前端 import**

把 `src/note/` 下所有 `from "./blocks/ranges"`、`from "./tags/model"`、`from "./tasks"`（及其带 `.js`/相对层级的形式）改为 `from "@floatnote/note-logic"`。用 grep 定位：
```bash
grep -rln "blocks/ranges\|tags/model\|\"./tasks\"\|'./tasks'" src
```
逐文件改 import。

- [ ] **Step 5: 跑测试确认行为不变**

Run: `npm test`
Expected: 全部既有测试通过（搬迁不改逻辑）。

Run: `npm run build`
Expected: tsc 类型检查通过。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "refactor: extract @floatnote/note-logic shared package

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: 共享包加匹配算法（anchor 定位 + str_replace）

为 `edit_note`/`set_tag` 提供纯函数：唯一性校验的 str_replace、归一化前缀定位块。TDD。

**Files:**
- Create: `shared/note-logic/src/matching.ts`
- Create: `shared/note-logic/src/matching.test.ts`

**Interfaces:**
- Consumes: `blockRanges`、`stripTagMarker` from `@floatnote/note-logic`。
- Produces:
  - `replaceOnce(doc: string, oldString: string, newString: string): { ok: true; newContent: string } | { ok: false; error: string }` — 唯一匹配则替换，0 或 >1 返回错误。
  - `findBlockByAnchor(doc: string, anchor: string): { ok: true; range: BlockRange } | { ok: false; error: string }` — 归一化（剥标记）后找以 anchor 开头的块，要求唯一。

- [ ] **Step 1: 写失败测试**

Create `shared/note-logic/src/matching.test.ts`:
```typescript
import { describe, it, expect } from "vitest";
import { replaceOnce, findBlockByAnchor } from "./matching.js";

describe("replaceOnce", () => {
  it("replaces a unique match", () => {
    const r = replaceOnce("a\nb\nc", "b", "B");
    expect(r).toEqual({ ok: true, newContent: "a\nB\nc" });
  });
  it("rejects zero matches", () => {
    expect(replaceOnce("a\nb", "z", "Z")).toEqual({ ok: false, error: expect.any(String) });
  });
  it("rejects multiple matches", () => {
    expect(replaceOnce("a a a", "a", "b")).toEqual({ ok: false, error: expect.any(String) });
  });
});

describe("findBlockByAnchor", () => {
  it("locates a block by unique prefix", () => {
    const doc = "第一块\n\n第二块\n\n第三块";
    const r = findBlockByAnchor(doc, "第二");
    expect(r.ok).toBe(true);
    expect(doc.slice((r as any).range.from, (r as any).range.to)).toBe("第二块");
  });
  it("strips tag markers before matching", () => {
    const doc = "第一块<!-- floatnote:tag=review -->\n\n第二块";
    const r = findBlockByAnchor(doc, "第一块");
    expect(r.ok).toBe(true);
  });
  it("rejects non-unique anchor", () => {
    const doc = "复习\n\n复习";
    expect(findBlockByAnchor(doc, "复习").ok).toBe(false);
  });
  it("rejects zero matches", () => {
    expect(findBlockByAnchor("a\n\nb", "z").ok).toBe(false);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd shared/note-logic && npx vitest run src/matching.test.ts`（或根 `npm test -- matching`）
Expected: FAIL — 模块不存在。

- [ ] **Step 3: 实现**

Create `shared/note-logic/src/matching.ts`:
```typescript
import { blockRanges, stripTagMarker, type BlockRange } from "./index.js";

export type ReplaceResult =
  | { ok: true; newContent: string }
  | { ok: false; error: string };

/** Replace the single unique occurrence of `oldString` in `doc`. */
export function replaceOnce(doc: string, oldString: string, newString: string): ReplaceResult {
  if (oldString.length === 0) return { ok: false, error: "old_string 不能为空" };
  const idx = doc.indexOf(oldString);
  if (idx === -1) return { ok: false, error: "未找到要替换的文本" };
  if (doc.indexOf(oldString, idx + 1) !== -1) {
    return { ok: false, error: "要替换的文本不唯一，请补充更多上下文" };
  }
  const newContent = doc.slice(0, idx) + newString + doc.slice(idx + oldString.length);
  return { ok: true, newContent };
}

export type AnchorResult =
  | { ok: true; range: BlockRange }
  | { ok: false; error: string };

/** Find the unique block whose normalized text starts with `anchor`. */
export function findBlockByAnchor(doc: string, anchor: string): AnchorResult {
  if (anchor.length === 0) return { ok: false, error: "anchor 不能为空" };
  const matches = blockRanges(doc).filter((range) => {
    const text = stripTagMarker(doc.slice(range.from, range.to));
    return text.startsWith(anchor);
  });
  if (matches.length === 0) return { ok: false, error: "未找到匹配的块" };
  if (matches.length > 1) return { ok: false, error: "anchor 匹配到多个块，请补充上下文" };
  return { ok: true, range: matches[0] };
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm test`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat(note-logic): add anchor + str_replace matching helpers

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: sidecar 协议加 ApplyEdit/GetNoteText

废弃 `apply_write`/`apply_write_result`，加 `apply_edit`/`apply_edit_result`/`get_note_text`/`note_text`。TDD。

**Files:**
- Modify: `sidecar/src/protocol.ts`
- Modify: `sidecar/src/protocol.test.ts`

**Interfaces:**
- Produces（`HostToSidecar` 新变体）:
  - `{ type: "apply_edit_result"; callId: string; ok: boolean; denied?: boolean; version?: number; error?: string }`
  - `{ type: "note_text"; callId: string; content: string; found: boolean }`
- Produces（`SidecarToHost` 新变体）:
  - `{ type: "apply_edit"; callId: string; conversationId: string; target: NoteTarget; toolName: string; oldContent: string; newContent: string; preview: EditPreview }`
  - `{ type: "get_note_text"; callId: string; conversationId: string; target: NoteTarget }`
- Produces（导出类型）:
  - `NoteTarget = { kind: "inbox" | "tasks" | "piece" | "doc"; name?: string }`
  - `EditPreview = { tool: string; summary: string; detail: EditPreviewDetail }`
  - `EditPreviewDetail = { kind: "diff"; hunks: string } | { kind: "tag_assign"; blockPreview: string; tagName: string; tagColor: string } | { kind: "tag_create"; tagName: string; tagColor: string } | { kind: "tag_delete"; tagName: string; markerCount: number }`

- [ ] **Step 1: 写失败测试**

Append to `sidecar/src/protocol.test.ts`:
```typescript
import { encodeLine } from "./protocol.js";
import type { SidecarToHost, HostToSidecar, NoteTarget } from "./protocol.js";

describe("apply_edit protocol", () => {
  const target: NoteTarget = { kind: "inbox" };
  it("encodes apply_edit with preview", () => {
    const msg: SidecarToHost = {
      type: "apply_edit",
      callId: "w1",
      conversationId: "c1",
      target,
      toolName: "set_tag",
      oldContent: "a",
      newContent: "b",
      preview: { tool: "set_tag", summary: "打标签", detail: { kind: "tag_assign", blockPreview: "块", tagName: "review", tagColor: "#e5484d" } },
    };
    const line = encodeLine(msg);
    expect(line).toContain('"type":"apply_edit"');
    expect(line).toContain('"toolName":"set_tag"');
    expect(line.endsWith("\n")).toBe(true);
  });
  it("decodes apply_edit_result with denied", () => {
    const line = encodeLine({ type: "apply_edit_result", callId: "w1", ok: false, denied: true } as HostToSidecar);
    expect(JSON.parse(line)).toMatchObject({ type: "apply_edit_result", callId: "w1", denied: true });
  });
  it("encodes get_note_text / note_text", () => {
    const req: SidecarToHost = { type: "get_note_text", callId: "g1", conversationId: "c1", target };
    const res: HostToSidecar = { type: "note_text", callId: "g1", content: "doc", found: true };
    expect(encodeLine(req)).toContain('"type":"get_note_text"');
    expect(encodeLine(res)).toContain('"type":"note_text"');
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && npm test`
Expected: FAIL — 类型不存在。

- [ ] **Step 3: 改 protocol.ts**

在 `sidecar/src/protocol.ts` 加导出类型并替换 `HostToSidecar`/`SidecarToHost` 中的 `apply_write`/`apply_write_result`：

```typescript
export type NoteTarget = { kind: "inbox" | "tasks" | "piece" | "doc"; name?: string };

export type EditPreviewDetail =
  | { kind: "diff"; hunks: string }
  | { kind: "tag_assign"; blockPreview: string; tagName: string; tagColor: string }
  | { kind: "tag_create"; tagName: string; tagColor: string }
  | { kind: "tag_delete"; tagName: string; markerCount: number };

export interface EditPreview {
  tool: string;
  summary: string;
  detail: EditPreviewDetail;
}
```

把 `HostToSidecar` 的 `apply_write_result` 分支改为：
```typescript
  | {
      type: "apply_edit_result";
      callId: string;
      ok: boolean;
      denied?: boolean;
      version?: number;
      error?: string;
    }
  | { type: "note_text"; callId: string; content: string; found: boolean }
```
把 `SidecarToHost` 的 `apply_write` 分支改为：
```typescript
  | {
      type: "apply_edit";
      callId: string;
      conversationId: string;
      target: NoteTarget;
      toolName: string;
      oldContent: string;
      newContent: string;
      preview: EditPreview;
    }
  | { type: "get_note_text"; callId: string; conversationId: string; target: NoteTarget }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd sidecar && npm test`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat(sidecar): apply_edit + get_note_text protocol

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: Rust 协议枚举对齐

`src-tauri/src/agent.rs` 的 `HostToSidecar`/`SidecarToHost` 加对应变体，序列化字段 camelCase 对齐。

**Files:**
- Modify: `src-tauri/src/agent.rs`（`HostToSidecar`、`SidecarToHost`、`SidecarToHost::ApplyWrite` 相关 match 分支）

**Interfaces:**
- Produces: Rust 镜像类型，字段 `rename_all = "camelCase"`，与 sidecar `protocol.ts` 字段名一致。`NoteTarget`、`EditPreview`、`EditPreviewDetail` 用 `#[serde(tag = "kind")]`。

- [ ] **Step 1: 写失败测试**

在 `src-tauri/src/agent.rs` 的 `#[cfg(test)]` 模块加：
```rust
    #[test]
    fn parses_apply_edit_line() {
        let line = r#"{"type":"apply_edit","callId":"w1","conversationId":"c1","target":{"kind":"inbox"},"toolName":"set_tag","oldContent":"a","newContent":"b","preview":{"tool":"set_tag","summary":"s","detail":{"kind":"tag_assign","blockPreview":"块","tagName":"review","tagColor":"#e5484d"}}}"#;
        let msg: SidecarToHost = serde_json::from_str(line).unwrap();
        match msg {
            SidecarToHost::ApplyEdit { tool_name, .. } => assert_eq!(tool_name, "set_tag"),
            _ => panic!("not ApplyEdit"),
        }
    }

    #[test]
    fn serializes_apply_edit_result_denied() {
        let msg = HostToSidecar::ApplyEditResult {
            call_id: "w1".into(),
            ok: false,
            denied: Some(true),
            version: None,
            error: None,
        };
        let json = serde_json::to_string(&msg).unwrap();
        assert!(json.contains("\"type\":\"apply_edit_result\""), "{json}");
        assert!(json.contains("\"denied\":true"), "{json}");
    }

    #[test]
    fn parses_note_text_line() {
        let line = r#"{"type":"note_text","callId":"g1","content":"doc","found":true}"#;
        let msg: HostToSidecar::from_line(line);
        match msg.unwrap() {
            HostToSidecar::NoteText { call_id, found, .. } => {
                assert_eq!(call_id, "g1");
                assert!(found);
            }
            _ => panic!("not NoteText"),
        }
    }
```
（若 `from_line` 不存在，直接 `serde_json::from_str::<HostToSidecar>(line)`。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd src-tauri && cargo test --lib agent`
Expected: FAIL — 变体不存在。

- [ ] **Step 3: 改枚举**

把 `HostToSidecar` 的 `ApplyWriteResult` 改为：
```rust
    ApplyEditResult {
        call_id: String,
        ok: bool,
        #[serde(skip_serializing_if = "Option::is_none")]
        denied: Option<bool>,
        #[serde(skip_serializing_if = "Option::is_none")]
        version: Option<u32>,
        #[serde(skip_serializing_if = "Option::is_none")]
        error: Option<String>,
    },
    NoteText {
        call_id: String,
        content: String,
        found: bool,
    },
```
把 `SidecarToHost` 的 `ApplyWrite` 改为：
```rust
    ApplyEdit {
        call_id: String,
        conversation_id: String,
        target: NoteTarget,
        tool_name: String,
        old_content: String,
        new_content: String,
        preview: EditPreview,
    },
    GetNoteText {
        call_id: String,
        conversation_id: String,
        target: NoteTarget,
    },
```
新增类型：
```rust
#[derive(Deserialize, Serialize, Debug, Clone, PartialEq)]
#[serde(rename_all = "camelCase")]
pub struct NoteTarget {
    pub kind: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

#[derive(Deserialize, Serialize, Debug, Clone, PartialEq)]
#[serde(tag = "kind", rename_all = "camelCase")]
pub enum EditPreviewDetail {
    Diff { hunks: String },
    TagAssign { block_preview: String, tag_name: String, tag_color: String },
    TagCreate { tag_name: String, tag_color: String },
    TagDelete { tag_name: String, marker_count: u32 },
}

#[derive(Deserialize, Serialize, Debug, Clone, PartialEq)]
#[serde(rename_all = "camelCase")]
pub struct EditPreview {
    pub tool: String,
    pub summary: String,
    pub detail: EditPreviewDetail,
}
```
`handle_sidecar_msg` 里把 `SidecarToHost::ApplyWrite { .. }` 分支改为 `SidecarToHost::ApplyEdit { .. }` 与 `SidecarToHost::GetNoteText { .. }`，先指向占位 `handle_apply_edit`/`handle_get_note_text`（下一任务实现，本任务可 `todo!()` 或先转发占位让编译通过、测试只验序列化）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd src-tauri && cargo test --lib agent`
Expected: PASS。

Run: `cd src-tauri && cargo check`
Expected: 通过。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat(agent): mirror apply_edit + note_text protocol in Rust

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: Rust apply_edit + get_note_text + permission 往返

实现 `handle_apply_edit`（target 解析、并发校验、write_mode、快照守卫、落盘、广播、回结果）、`handle_get_note_text`、`permission://request` emit、`resolve_permission` 命令。TDD（tempdir）。

**Files:**
- Modify: `src-tauri/src/agent.rs`（`handle_apply_edit`、`handle_get_note_text`、`ActiveNote` 解析 helper）
- Modify: `src-tauri/src/commands.rs`（`resolve_permission` 命令、pending permission 表）
- Modify: `src-tauri/src/lib.rs`（注册命令 `resolve_permission`）

**Interfaces:**
- Consumes: `AppState`（`active_note: Mutex<Option<ActiveNote>>`、`write_suppress`、`config`），`versions::snapshot`，`watcher::mark_self_write`。
- Produces:
  - Tauri 事件 `permission://request`，载荷 `{ request_id, conversation_id, note_id, target, tool_name, old_content, new_content, preview, can_snapshot: bool }`。
  - 命令 `resolve_permission(request_id: String, decision: "allow"|"deny", write_mode: "direct"|"snapshot")`。
  - `handle_apply_edit` 返回 `HostToSidecar::ApplyEditResult`。
  - `handle_get_note_text` 返回 `HostToSidecar::NoteText`。

- [ ] **Step 1: 写失败测试**

在 `agent.rs` 测试模块加（用现成 `tempdir()` fixture）：
```rust
    #[test]
    fn apply_edit_direct_writes_without_snapshot() {
        let dir = tempdir();
        let path = dir.path().join("piece.md");
        std::fs::write(&path, "old").unwrap();
        // direct 模式：不快照
        let res = handle_apply_edit_at(&dir.path().to_path_buf(), "piece", &path, "old", "new", "direct", true);
        assert!(res.ok);
        assert_eq!(res.version, None);
        assert_eq!(std::fs::read_to_string(&path).unwrap(), "new");
    }

    #[test]
    fn apply_edit_snapshot_for_piece_creates_version() {
        let dir = tempdir();
        let path = dir.path().join("piece.md");
        std::fs::write(&path, "old").unwrap();
        let res = handle_apply_edit_at(&dir.path().to_path_buf(), "piece", &path, "old", "new", "snapshot", true);
        assert!(res.ok);
        assert_eq!(res.version, Some(1));
        assert_eq!(versions::read_version(dir.path(), "piece", 1).unwrap(), "old");
    }

    #[test]
    fn apply_edit_snapshot_ignored_for_inbox() {
        let dir = tempdir();
        let path = dir.path().join("_inbox.md");
        std::fs::write(&path, "old").unwrap();
        let res = handle_apply_edit_at(&dir.path().to_path_buf(), "_inbox", &path, "old", "new", "snapshot", false);
        assert!(res.ok);
        assert_eq!(res.version, None); // _ 前缀不快照
        assert_eq!(std::fs::read_to_string(&path).unwrap(), "new");
    }

    #[test]
    fn apply_edit_rejects_concurrent_change() {
        let dir = tempdir();
        let path = dir.path().join("piece.md");
        std::fs::write(&path, "user changed").unwrap(); // 磁盘已变
        let res = handle_apply_edit_at(&dir.path().to_path_buf(), "piece", &path, "old", "new", "direct", true);
        assert!(!res.ok);
        assert!(res.error.unwrap().contains("已变更"));
    }
```
（`handle_apply_edit_at` 是抽出路径参数的纯函数版本，便于测试；真实 `handle_apply_edit` 从 `AppState` 取 active note 后委托给它。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd src-tauri && cargo test --lib agent`
Expected: FAIL — 函数不存在。

- [ ] **Step 3: 实现核心函数**

在 `agent.rs`：
```rust
pub struct EditOutcome { pub ok: bool, pub denied: bool, pub version: Option<u32>, pub error: Option<String> }

/// 解析 target → (dir, note_id, path)。piece/doc 需在当前项目空间下；
/// inbox/tasks 用项目空间根 + 固定文件名。解析失败返回 None。
fn resolve_target(state: &AppState, target: &NoteTarget) -> Option<(PathBuf, String, PathBuf)> {
    let active = state.active_note.lock().unwrap().clone();
    let active = match (active, &target.name) {
        (Some(n), _) => n,
        _ => return None,
    };
    // 简化：target.name 缺省=active；显式 inbox/tasks 用 _inbox/_tasks。
    let (note_id, path) = match target.kind.as_str() {
        "inbox" => ("_inbox".to_string(), active.dir.join("_inbox.md")),
        "tasks" => ("_tasks".to_string(), active.dir.join("_tasks.md")),
        "piece" | "doc" | _ => (target.name.clone().unwrap_or(active.note_id), active.path.clone()),
    };
    Some((PathBuf::from(active.dir), note_id, path))
}

pub fn handle_apply_edit_at(
    dir: &Path, note_id: &str, path: &Path,
    old_content: &str, new_content: &str, write_mode: &str, can_snapshot: bool,
) -> EditOutcome {
    // 并发校验
    let on_disk = std::fs::read_to_string(path).unwrap_or_default();
    if on_disk != old_content {
        return EditOutcome { ok: false, denied: false, version: None, error: Some("笔记已变更，请重读".into()) };
    }
    // 快照守卫：仅 piece 且 write_mode=snapshot
    let version = if write_mode == "snapshot" && can_snapshot && !note_id.starts_with('_') {
        match versions::snapshot(dir, note_id, old_content, "ai") {
            Ok(v) => Some(v),
            Err(_) => None,
        }
    } else { None };
    if let Err(e) = std::fs::write(path, new_content) {
        return EditOutcome { ok: false, denied: false, version: None, error: Some(e.to_string()) };
    }
    EditOutcome { ok: true, denied: false, version, error: None }
}
```
`handle_apply_edit`（在 `handle_sidecar_msg` 调用，需 `app` 与全部字段）：
```rust
fn handle_apply_edit(app: &AppHandle, call_id: String, conversation_id: String,
    target: NoteTarget, tool_name: String, old_content: String, new_content: String, preview: EditPreview) {
    let Some(state) = app.try_state::<AppState>() else { return; };
    let can_snapshot = target.kind == "piece";
    let resolved = resolve_target(&state, &target);
    // 发权限请求给前端；用户决策后在 resolve_permission 里完成落盘并回结果。
    let request_id = call_id.clone();
    let payload = serde_json::json!({
        "request_id": request_id,
        "conversation_id": conversation_id,
        "target": target,
        "tool_name": tool_name,
        "old_content": old_content,
        "new_content": new_content,
        "preview": preview,
        "can_snapshot": can_snapshot,
        "resolved_path": resolved.as_ref().map(|(_, _, p)| p.to_string_lossy().to_string()),
        "resolved_dir": resolved.as_ref().map(|(d, _, _)| d.to_string_lossy().to_string()),
        "resolved_note_id": resolved.as_ref().map(|(_, n, _)| n.clone()),
    });
    let _ = app.emit("permission://request", &payload);
    // 存 pending：request_id → (state 需要的字段)，resolve_permission 取出落盘。
    state.pending_edits.lock().unwrap().insert(request_id, PendingEdit {
        call_id, conversation_id, target, old_content, new_content, write_mode: "direct".into(),
    });
}
```
在 `AppState` 加字段（`commands.rs`）：
```rust
pub struct PendingEdit {
    pub call_id: String,
    pub conversation_id: String,
    pub target: crate::agent::NoteTarget,
    pub old_content: String,
    pub new_content: String,
    pub write_mode: String,
}
pub pending_edits: Mutex<std::collections::HashMap<String, PendingEdit>>,
```
`handle_get_note_text`：
```rust
fn handle_get_note_text(app: &AppHandle, call_id: String, conversation_id: String, target: NoteTarget) {
    let Some(state) = app.try_state::<AppState>() else { return; };
    let (content, found) = match resolve_target(&state, &target) {
        Some((_, _, path)) => match std::fs::read_to_string(&path) {
            Ok(c) => (c, true),
            Err(_) => (String::new(), false),
        },
        None => (String::new(), false),
    };
    let _ = state.agent.lock().unwrap().as_mut().map(|a| a.send(&HostToSidecar::NoteText { call_id, content, found }));
}
```
`commands.rs` 加命令：
```rust
#[tauri::command]
pub fn resolve_permission(
    state: State<AppState>,
    request_id: String,
    decision: String,
    write_mode: String,
) -> Result<(), String> {
    let app = state.app.clone(); // 若 AppState 未存 app，改用 AppHandle 参数
    let pending = state.pending_edits.lock().unwrap().remove(&request_id);
    let Some(p) = pending else { return Ok(()); };
    if decision != "allow" {
        let _ = state.agent.lock().unwrap().as_mut().map(|a| a.send(&HostToSidecar::ApplyEditResult {
            call_id: p.call_id, ok: false, denied: Some(true), version: None, error: None,
        }));
        return Ok(());
    }
    let can_snapshot = p.target.kind == "piece";
    let (dir, note_id, path) = /* 从 pending 里重算 resolved target（同 resolve_target，但 PendingEdit 已存 target + 在 handle_apply_edit 存了 resolved_dir/note_id/path，这里直接用） */;
    crate::watcher::mark_self_write(&state.write_suppress, &path);
    let outcome = crate::agent::handle_apply_edit_at(&dir, &note_id, &path, &p.old_content, &p.new_content, &write_mode, can_snapshot);
    let _ = app.emit("note://updated", /* NoteUpdated { note_id, path, version } */);
    let _ = state.agent.lock().unwrap().as_mut().map(|a| a.send(&HostToSidecar::ApplyEditResult {
        call_id: p.call_id, ok: outcome.ok, denied: false, version: outcome.version, error: outcome.error,
    }));
    Ok(())
}
```
> 注：`AppState` 需能拿到 `AppHandle`（在 `lib.rs` setup 时注入，或命令签名加 `app: AppHandle`）。`PendingEdit` 应在 `handle_apply_edit` 里把 resolved 的 `dir/note_id/path` 也存进去，避免 `resolve_permission` 再算。落地时把这三个字段补进 `PendingEdit`。

`lib.rs` 注册命令：
```rust
.invoke_handler(tauri::generate_handler![
    // ...既有...
    commands::resolve_permission,
])
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd src-tauri && cargo test --lib agent`
Expected: PASS（4 个新测试）。

Run: `cd src-tauri && cargo check`
Expected: 通过。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat(agent): apply_edit with permission round-trip + concurrency guard

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: 重写 sidecar note-tools 为 7 工具

用共享包 + 扩展后的 deps 实现 7 个工具，每个工具 pull 全文→算变换→buildPreview→requestWrite。

**Files:**
- Modify: `sidecar/src/note-tools.ts`
- Modify: `sidecar/src/note-tools.test.ts`

**Interfaces:**
- Consumes: `replaceOnce`/`findBlockByAnchor`/`setBlockTagChange`/`addTagDefChange`/`deleteTagChanges`/`blockTagId`/`stripTagMarker`/`parseDefs` from `@floatnote/note-logic`；`defineTool` from Pi。
- Produces: `createNoteTools(deps)` 返回 7 个 `ToolDefinition`。`NoteToolDeps` 改为：
  ```typescript
  export interface NoteToolDeps {
    getNoteText: (target?: NoteTarget) => Promise<string>;
    requestWrite: (args: { target?: NoteTarget; toolName: string; oldContent: string; newContent: string; preview: EditPreview }) => Promise<WriteResult>;
  }
  ```
  `WriteResult` 加 `denied?: boolean`。

- [ ] **Step 1: 写失败测试**

Rewrite `sidecar/src/note-tools.test.ts`:
```typescript
import { describe, it, expect, vi } from "vitest";
import { createNoteTools, type NoteToolDeps } from "./note-tools.js";
import type { NoteTarget, EditPreview } from "./protocol.js";

function makeDeps(noteText: string): { deps: NoteToolDeps; writes: any[] } {
  const writes: any[] = [];
  const deps: NoteToolDeps = {
    getNoteText: async () => noteText,
    requestWrite: async (args) => {
      writes.push(args);
      return { ok: true, version: 1 };
    },
  };
  return { deps, writes };
}

const inbox: NoteTarget = { kind: "inbox" };

describe("edit_note", () => {
  it("replaces unique substring", async () => {
    const { deps, writes } = makeDups("a\nb\nc");
    const tools = createNoteTools(deps);
    const edit = tools.find((t) => t.name === "edit_note")!;
    await (edit as any).execute("id", { old_string: "b", new_string: "B" });
    expect(writes[0].newContent).toBe("a\nB\nc");
    expect(writes[0].preview.detail.kind).toBe("diff");
  });
  it("errors on non-unique", async () => {
    const { deps } = makeDups("a a a");
    const tools = createNoteTools(deps);
    const edit = tools.find((t) => t.name === "edit_note")!;
    const r = await (edit as any).execute("id", { old_string: "a", new_string: "b" });
    expect(r.content[0].text).toContain("不唯一");
  });
});

describe("set_tag", () => {
  it("attaches tag marker to anchored block", async () => {
    const note = "<!-- floatnote-tags: review=\"复习\"|c=#e5484d -->\n\n第一块\n\n第二块";
    const { deps, writes } = makeDups(note);
    const tools = createNoteTools(deps);
    const set = tools.find((t) => t.name === "set_tag")!;
    await (set as any).execute("id", { anchor: "第二块", tagId: "review" });
    expect(writes[0].newContent).toContain("<!-- floatnote:tag=review -->");
    expect(writes[0].preview.detail.kind).toBe("tag_assign");
  });
});

describe("tag_create", () => {
  it("adds defs entry", async () => {
    const { deps, writes } = makeDups("第一块");
    const tools = createNoteTools(deps);
    const c = tools.find((t) => t.name === "tag_create")!;
    await (c as any).execute("id", { name: "重点", color: "#e5484d" });
    expect(writes[0].newContent).toContain("重点");
  });
});

describe("list_tags", () => {
  it("returns defs and free colors", async () => {
    const note = "<!-- floatnote-tags: review=\"复习\"|c=#e5484d -->\n第一块";
    const { deps } = makeDups(note);
    const tools = createNoteTools(deps);
    const lt = tools.find((t) => t.name === "list_tags")!;
    const r = await (lt as any).execute("id", {});
    expect(r.content[0].text).toContain("review");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && npm test`
Expected: FAIL — 旧实现不匹配。

- [ ] **Step 3: 重写 note-tools.ts**

```typescript
import { defineTool, type ToolDefinition } from "@earendil-works/pi-coding-agent";
import { Type } from "typebox";
import {
  replaceOnce, findBlockByAnchor, setBlockTagChange, addTagDefChange,
  deleteTagChanges, parseDefs, stripTagMarker, blockTagId, type BlockRange,
} from "@floatnote/note-logic";
import type { NoteTarget, EditPreview } from "./protocol.js";

export interface WriteResult { ok: boolean; version?: number; denied?: boolean; error?: string; }

export interface NoteToolDeps {
  getNoteText: (target?: NoteTarget) => Promise<string>;
  requestWrite: (args: { target?: NoteTarget; toolName: string; oldContent: string; newContent: string; preview: EditPreview }) => Promise<WriteResult>;
}

const PALETTE = ["#e5484d", "#f5a524", "#15b395", "#4c9eeb", "#a78bfa", "#ec4899"];

function freeColors(used: Set<string>): string[] {
  return PALETTE.filter((c) => !used.has(c.toLowerCase()));
}

export function createNoteTools(deps: NoteToolDeps): ToolDefinition[] {
  const readNote = defineTool({
    name: "read_note",
    label: "Read note",
    description: "读取目标笔记的全文。target 缺省=当前活动笔记。",
    parameters: Type.Object({ target: Type.Optional(Type.Object({ kind: Type.String(), name: Type.Optional(Type.String()) })) }),
    promptSnippet: "read_note — 读笔记全文",
    async execute(_id, params: { target?: NoteTarget }) {
      const content = await deps.getNoteText(params.target);
      return { content: [{ type: "text", text: content }], details: {} };
    },
  });

  const listTags = defineTool({
    name: "list_tags",
    label: "List tags",
    description: "列出采集区（_inbox）已定义的标签与可用颜色。仅在 target=inbox 时有意义。",
    parameters: Type.Object({ target: Type.Optional(Type.Object({ kind: Type.String(), name: Type.Optional(Type.String()) })) }),
    promptSnippet: "list_tags — 列标签与可用颜色",
    async execute(_id, params: { target?: NoteTarget }) {
      const doc = await deps.getNoteText({ kind: "inbox", ...(params.target?.name ? { name: params.target.name } : {}) });
      const map = parseDefs(doc);
      const used = new Set([...map.values()].map((d) => d.color.toLowerCase()));
      const tags = [...map.values()].map((d) => ({ id: d.id, name: d.name, color: d.color }));
      const free = freeColors(used);
      return { content: [{ type: "text", text: JSON.stringify({ tags, freeColors: free }) }], details: {} };
    },
  });

  const editNote = defineTool({
    name: "edit_note",
    label: "Edit note",
    description: "精确替换目标笔记中唯一出现的 old_string 为 new_string。用于改块/插块/删块/改任务。old_string 必须唯一，否则报错。",
    parameters: Type.Object({
      old_string: Type.String({ description: "要替换的原文，必须在全文中唯一" }),
      new_string: Type.String({ description: "替换后的新文本" }),
      target: Type.Optional(Type.Object({ kind: Type.String(), name: Type.Optional(Type.String()) })),
    }),
    promptSnippet: "edit_note — 唯一 str_replace",
    async execute(_id, params: { old_string: string; new_string: string; target?: NoteTarget }) {
      const old = await deps.getNoteText(params.target);
      const r = replaceOnce(old, params.old_string, params.new_string);
      if (!r.ok) return { content: [{ type: "text", text: `替换失败：${r.error}` }], details: {} };
      const preview: EditPreview = { tool: "edit_note", summary: "编辑文本", detail: { kind: "diff", hunks: unifiedDiff(old, r.newContent) } };
      const res = await deps.requestWrite({ target: params.target, toolName: "edit_note", oldContent: old, newContent: r.newContent, preview });
      return writeResultText(res);
    },
  });

  const writeNote = defineTool({
    name: "write_note",
    label: "Write note",
    description: "整篇覆写目标笔记。仅在大重构/跨多块改写时用。每次写入前用户可选择保存旧版本快照。",
    parameters: Type.Object({
      content: Type.String({ description: "新全文" }),
      target: Type.Optional(Type.Object({ kind: Type.String(), name: Type.Optional(Type.String()) })),
    }),
    promptSnippet: "write_note — 整篇覆写",
    async execute(_id, params: { content: string; target?: NoteTarget }) {
      const old = await deps.getNoteText(params.target);
      const preview: EditPreview = { tool: "write_note", summary: "整篇覆写", detail: { kind: "diff", hunks: unifiedDiff(old, params.content) } };
      const res = await deps.requestWrite({ target: params.target, toolName: "write_note", oldContent: old, newContent: params.content, preview });
      return writeResultText(res);
    },
  });

  const setTag = defineTool({
    name: "set_tag",
    label: "Set tag",
    description: "给采集区某块打/清标签。anchor=块首行前缀（唯一）。tagId=null 清除。target 必须 inbox。",
    parameters: Type.Object({
      anchor: Type.String({ description: "块首行前缀，须唯一匹配一个块" }),
      tagId: Type.Optional(Type.String({ description: "标签 id；省略或空串=清除该块标签" })),
      target: Type.Optional(Type.Object({ kind: Type.String(), name: Type.Optional(Type.String()) })),
    }),
    promptSnippet: "set_tag — 给块打标签",
    async execute(_id, params: { anchor: string; tagId?: string; target?: NoteTarget }) {
      const target: NoteTarget = { kind: "inbox", ...(params.target?.name ? { name: params.target.name } : {}) };
      const old = await deps.getNoteText(target);
      const r = findBlockByAnchor(old, params.anchor);
      if (!r.ok) return { content: [{ type: "text", text: `定位块失败：${r.error}` }], details: {} };
      const change = setBlockTagChange(old, r.range, params.tagId || null);
      const newContent = change ? applyChange(old, change) : old;
      const tagName = params.tagId ? (parseDefs(old).get(params.tagId)?.name ?? params.tagId) : "(清除)";
      const tagColor = params.tagId ? (parseDefs(old).get(params.tagId)?.color ?? "#888") : "#888";
      const blockPreview = old.slice(r.range.from, r.range.to).split("\n")[0].slice(0, 20);
      const preview: EditPreview = { tool: "set_tag", summary: `给块「${blockPreview}」${params.tagId ? "打上" : "清除"}标签`, detail: { kind: "tag_assign", blockPreview, tagName, tagColor } };
      const res = await deps.requestWrite({ target, toolName: "set_tag", oldContent: old, newContent, preview });
      return writeResultText(res);
    },
  });

  const tagCreate = defineTool({
    name: "tag_create",
    label: "Create tag",
    description: "在采集区新建标签定义。color 必须从 list_tags 返回的 freeColors 中选一个。target 必须 inbox。",
    parameters: Type.Object({
      name: Type.String({ description: "标签名" }),
      color: Type.String({ description: "颜色 hex，须为可用色" }),
      target: Type.Optional(Type.Object({ kind: Type.String(), name: Type.Optional(Type.String()) })),
    }),
    promptSnippet: "tag_create — 新建标签",
    async execute(_id, params: { name: string; color: string; target?: NoteTarget }) {
      const target: NoteTarget = { kind: "inbox", ...(params.target?.name ? { name: params.target.name } : {}) };
      const old = await deps.getNoteText(target);
      const r = addTagDefChange(old, params.name, params.color);
      if (!r.id) return { content: [{ type: "text", text: `建标签失败：颜色 ${params.color} 已被占用；可用：${JSON.stringify(freeColors(new Set([...parseDefs(old).values()].map((d) => d.color.toLowerCase()))))}` }], details: {} };
      const newContent = r.change ? applyChange(old, r.change) : old;
      const preview: EditPreview = { tool: "tag_create", summary: `新建标签「${params.name}」`, detail: { kind: "tag_create", tagName: params.name, tagColor: params.color } };
      const res = await deps.requestWrite({ target, toolName: "tag_create", oldContent: old, newContent, preview });
      return writeResultText(res);
    },
  });

  const tagDelete = defineTool({
    name: "tag_delete",
    label: "Delete tag",
    description: "删除采集区某标签定义及其所有块标记。target 必须 inbox。",
    parameters: Type.Object({
      tagId: Type.String({ description: "要删除的标签 id" }),
      target: Type.Optional(Type.Object({ kind: Type.String(), name: Type.Optional(Type.String()) })),
    }),
    promptSnippet: "tag_delete — 删标签",
    async execute(_id, params: { tagId: string; target?: NoteTarget }) {
      const target: NoteTarget = { kind: "inbox", ...(params.target?.name ? { name: params.target.name } : {}) };
      const old = await deps.getNoteText(target);
      const changes = deleteTagChanges(old, params.tagId);
      const newContent = applyChanges(old, changes);
      const def = parseDefs(old).get(params.tagId);
      const markerCount = countMarkers(old, params.tagId);
      const preview: EditPreview = { tool: "tag_delete", summary: `删除标签「${def?.name ?? params.tagId}」`, detail: { kind: "tag_delete", tagName: def?.name ?? params.tagId, markerCount } };
      const res = await deps.requestWrite({ target, toolName: "tag_delete", oldContent: old, newContent, preview });
      return writeResultText(res);
    },
  });

  return [readNote, listTags, editNote, writeNote, setTag, tagCreate, tagDelete];
}

function writeResultText(res: WriteResult) {
  if (res.denied) return { content: [{ type: "text", text: "用户拒绝了此操作" }], details: {} };
  if (res.ok) return { content: [{ type: "text", text: res.version ? `已更新，版本 v${res.version}` : "已更新" }], details: {} };
  return { content: [{ type: "text", text: `写入失败：${res.error ?? "未知错误"}` }], details: {} };
}

function applyChange(doc: string, c: { from: number; to: number; insert: string }): string {
  return doc.slice(0, c.from) + c.insert + doc.slice(c.to);
}
function applyChanges(doc: string, cs: { from: number; to: number; insert: string }[]): string {
  let out = doc;
  for (const c of [...cs].sort((a, b) => a.from - b.from)) out = applyChange(out, c);
  return out;
}
function countMarkers(doc: string, id: string): number {
  const re = new RegExp(`<!-- floatnote:tag=${id.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")} -->`, "g");
  return (doc.match(re) || []).length;
}
function unifiedDiff(a: string, b: string): string {
  // 轻量 diff：按行比对，足够气泡展示。首版用简单逐行 unified 形式。
  const la = a.split("\n"), lb = b.split("\n");
  const lines: string[] = [];
  const n = Math.max(la.length, lb.length);
  for (let i = 0; i < n; i++) {
    if (la[i] !== lb[i]) {
      if (la[i] !== undefined) lines.push(`- ${la[i]}`);
      if (lb[i] !== undefined) lines.push(`+ ${lb[i]}`);
    } else if (la[i] !== undefined) {
      lines.push(`  ${la[i]}`);
    }
  }
  return lines.join("\n");
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd sidecar && npm test`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat(sidecar): 7 note tools with structured writes

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: AgentRunner 接 getNoteText 往返与 ApplyEdit

把 `getNoteText` stub 实现为 `GetNoteText`/`NoteText` 往返；`requestWrite` 改发 `ApplyEdit` 并处理 `denied`；`createAgentSession` 的 `tools` 列表更新。

**Files:**
- Modify: `sidecar/src/agent.ts`
- Modify: `sidecar/src/agent.test.ts`
- Modify: `sidecar/src/main.ts`

**Interfaces:**
- Consumes: `NoteTarget`/`EditPreview`/`WriteResult`；`AgentRunner.onApplyWriteResult` 改名/扩展为 `onApplyEditResult`，并加 `onNoteText`。
- Produces: `AgentRunner` 新方法 `onNoteText(msg)`、`onApplyEditResult(msg)`；`requestWrite` 发 `ApplyEdit`。

- [ ] **Step 1: 写失败测试**

在 `sidecar/src/agent.test.ts` 加（沿用现有 mock `send` 的模式）：
```typescript
import { AgentRunner } from "./agent.js";

describe("AgentRunner getNoteText round-trip", () => {
  it("resolves getNoteText from note_text reply", async () => {
    const sent: any[] = [];
    const runner = new AgentRunner({ send: (m) => { sent.push(m); } });
    await runner.configure({ provider: "anthropic", model: "x" });
    await runner.newSession({ conversationId: "c1", cwd: ".", sessionDir: "." });
    const p = (runner as any).getNoteText("c1", { kind: "inbox" });
    expect(sent.at(-1).type).toBe("get_note_text");
    (runner as any).onNoteText({ type: "note_text", callId: sent.at(-1).callId, content: "doc", found: true });
    await expect(p).resolves.toBe("doc");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && npm test`
Expected: FAIL — `getNoteText` 不存在。

- [ ] **Step 3: 改 agent.ts**

把 `pendingWrites` 改名 `pendingEdits`，加 `pendingTexts`：
```typescript
private readonly pendingTexts = new Map<string, (r: { content: string; found: boolean }) => void>();
private textSeq = 0;
```
`getNoteText`：
```typescript
getNoteText(conversationId: string, target?: import("./protocol.js").NoteTarget): Promise<string> {
  const callId = `g${++this.textSeq}`;
  return new Promise((resolve) => {
    this.pendingTexts.set(callId, (r) => resolve(r.content));
    this.send({ type: "get_note_text", callId, conversationId, target: target ?? { kind: "inbox" } });
  });
}
onNoteText(msg: Extract<import("./protocol.js").HostToSidecar, { type: "note_text" }>): void {
  const r = this.pendingTexts.get(msg.callId);
  if (r) { this.pendingTexts.delete(msg.callId); r({ content: msg.content, found: msg.found }); }
}
```
`requestWrite` 改发 `ApplyEdit`：
```typescript
private requestWrite(conversationId: string, args: { target?: import("./protocol.js").NoteTarget; toolName: string; oldContent: string; newContent: string; preview: import("./protocol.js").EditPreview }): Promise<WriteResult> {
  const callId = `w${++this.writeSeq}`;
  return new Promise((resolve) => {
    this.pendingEdits.set(callId, resolve);
    this.send({ type: "apply_edit", callId, conversationId, target: args.target ?? { kind: "inbox" }, toolName: args.toolName, oldContent: args.oldContent, newContent: args.newContent, preview: args.preview });
  });
}
onApplyEditResult(msg: Extract<import("./protocol.js").HostToSidecar, { type: "apply_edit_result" }>): void {
  const r = this.pendingEdits.get(msg.callId);
  if (r) { this.pendingEdits.delete(msg.callId); r({ ok: msg.ok, denied: msg.denied, version: msg.version, error: msg.error }); }
}
```
`installSession` 里 `createNoteTools` 的 deps：
```typescript
const tools = createNoteTools({
  getNoteText: (target) => this.getNoteText(conversationId, target),
  requestWrite: (args) => this.requestWrite(conversationId, args),
});
```
`defaultCreateSession` 的 `tools: ["read_note", "write_note"]` 改为：
```typescript
tools: ["read_note", "list_tags", "edit_note", "write_note", "set_tag", "tag_create", "tag_delete"],
```
`sidecar/src/main.ts` 的 `handle`：把 `case "apply_write_result":` 改为：
```typescript
      case "apply_edit_result":
        runner.onApplyEditResult(msg);
        break;
      case "note_text":
        runner.onNoteText(msg);
        break;
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd sidecar && npm test`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat(sidecar): wire getNoteText + apply_edit round-trips

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 8: 前端权限气泡

订阅 `permission://request`，渲染 tool-aware 卡片，piece 才显示"保存快照后写入"下拉，调 `resolve_permission`。

**Files:**
- Create: `src/assistant/permission-bubble.ts`
- Create: `src/assistant/permission-bubble.test.ts`
- Modify: `src/assistant/assistant.ts`（挂载气泡 region）

**Interfaces:**
- Consumes: `invoke` from `@tauri-apps/api/core`、`listen` from `@tauri-apps/api/event`。`permission://request` 载荷见 Task 5。
- Produces: `mountPermissionBubble(root: HTMLElement): { destroy: () => void }`。

- [ ] **Step 1: 写失败测试**

Create `src/assistant/permission-bubble.test.ts`:
```typescript
import { describe, it, expect } from "vitest";
import { renderPreviewCard } from "./permission-bubble.js";

describe("renderPreviewCard", () => {
  it("renders diff card for edit_note", () => {
    const el = renderPreviewCard({ tool: "edit_note", summary: "s", detail: { kind: "diff", hunks: "- a\n+ b" } }, true);
    expect(el.textContent).toContain("编辑文本");
    expect(el.querySelector("pre")?.textContent).toContain("- a");
  });
  it("shows snapshot option only when can_snapshot", () => {
    const withSnap = renderPreviewCard({ tool: "edit_note", summary: "s", detail: { kind: "diff", hunks: "x" } }, true);
    const noSnap = renderPreviewCard({ tool: "edit_note", summary: "s", detail: { kind: "diff", hunks: "x" } }, false);
    expect(withSnap.querySelector("option[value='snapshot']")).toBeTruthy();
    expect(noSnap.querySelector("option[value='snapshot']")).toBeNull();
  });
  it("renders tag_assign card without raw marker", () => {
    const el = renderPreviewCard({ tool: "set_tag", summary: "s", detail: { kind: "tag_assign", blockPreview: "第一块", tagName: "review", tagColor: "#e5484d" } }, false);
    expect(el.textContent).toContain("review");
    expect(el.textContent).not.toContain("floatnote:tag");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm test -- permission-bubble`
Expected: FAIL — 模块不存在。

- [ ] **Step 3: 实现气泡**

Create `src/assistant/permission-bubble.ts`:
```typescript
import { invoke } from "@tauri-apps/api/core";
import { listen, type UnlistenFn } from "@tauri-apps/api/event";
import type { EditPreview } from "../../sidecar/src/protocol";

interface PermissionRequest {
  request_id: string;
  conversation_id: string;
  target: { kind: string; name?: string };
  tool_name: string;
  old_content: string;
  new_content: string;
  preview: EditPreview;
  can_snapshot: boolean;
}

/** 渲染单张 preview 卡片（纯 DOM，便于测试）。 */
export function renderPreviewCard(preview: EditPreview, canSnapshot: boolean): HTMLElement {
  const card = document.createElement("div");
  card.className = "perm-card";
  const title = document.createElement("div");
  title.className = "perm-title";
  title.textContent = preview.summary;
  card.appendChild(title);
  const detail = document.createElement("div");
  detail.className = "perm-detail";
  switch (preview.detail.kind) {
    case "diff": {
      const pre = document.createElement("pre");
      pre.textContent = preview.detail.hunks;
      detail.appendChild(pre);
      break;
    }
    case "tag_assign": {
      const row = document.createElement("div");
      const chip = document.createElement("span");
      chip.className = "tag-chip";
      chip.style.background = preview.detail.tagColor;
      chip.textContent = preview.detail.tagName;
      row.append("块「" + preview.detail.blockPreview + "」→ ", chip);
      detail.appendChild(row);
      break;
    }
    case "tag_create": {
      const chip = document.createElement("span");
      chip.className = "tag-chip";
      chip.style.background = preview.detail.tagColor;
      chip.textContent = preview.detail.tagName;
      detail.append("新建标签 ", chip);
      break;
    }
    case "tag_delete": {
      detail.textContent = `删除标签「${preview.detail.tagName}」，${preview.detail.markerCount} 处标记将清除`;
      break;
    }
  }
  card.appendChild(detail);
  const select = document.createElement("select");
  select.className = "perm-mode";
  const direct = document.createElement("option");
  direct.value = "direct";
  direct.textContent = "直接写入";
  select.appendChild(direct);
  if (canSnapshot) {
    const snap = document.createElement("option");
    snap.value = "snapshot";
    snap.textContent = "保存快照后写入";
    select.appendChild(snap);
  }
  card.appendChild(select);
  return card;
}

export function mountPermissionBubble(root: HTMLElement): { destroy: () => void } {
  root.classList.add("perm-bubble-root");
  let unlisten: UnlistenFn | null = null;
  let destroyed = false;

  function show(req: PermissionRequest) {
    root.replaceChildren();
    const card = renderPreviewCard(req.preview, req.can_snapshot);
    const allow = document.createElement("button");
    allow.textContent = "允许写入";
    const deny = document.createElement("button");
    deny.textContent = "拒绝";
    const select = card.querySelector<HTMLSelectElement>(".perm-mode")!;
    allow.addEventListener("click", () => {
      void invoke("resolve_permission", { requestId: req.request_id, decision: "allow", writeMode: select.value });
      root.replaceChildren();
    });
    deny.addEventListener("click", () => {
      void invoke("resolve_permission", { requestId: req.request_id, decision: "deny", writeMode: "direct" });
      root.replaceChildren();
    });
    root.append(card, allow, deny);
  }

  listen<PermissionRequest>("permission://request", (e) => show(e.payload)).then((un) => {
    if (destroyed) un(); else unlisten = un;
  });

  return {
    destroy() {
      destroyed = true;
      unlisten?.();
      root.classList.remove("perm-bubble-root");
      root.replaceChildren();
    },
  };
}
```

在 `src/assistant/assistant.ts` 的 `mountAssistant` 里，`assistant-dock` 内加 `<div class="assistant-perm-region"></div>`，并在末尾 `mountPermissionBubble(permRegion)`，`destroy()` 里调其 `destroy()`。需要把 `mountPermissionBubble` import 进来。

- [ ] **Step 4: 跑测试确认通过**

Run: `npm test -- permission-bubble`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat(assistant): permission bubble with tool-aware preview

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 9: 验证 AI 写入会话内可撤销（回归测试）

`setDoc` 已派发 history-tracked 事务，⌘Z 本应可撤。加回归测试锁定该行为，防止未来被改成静默重载。

**Files:**
- Modify: `src/note/editor.test.ts`

**Interfaces:**
- Consumes: `createEditor`、`setDoc`、`history()`（已隐含在 createEditor）。

- [ ] **Step 1: 写失败测试**

在 `src/note/editor.test.ts` 加：
```typescript
import { EditorView } from "@codemirror/view";
import { undo } from "@codemirror/commands";
import { createEditor, setDoc } from "./editor";

it("setDoc is undoable in-session", () => {
  const host = document.createElement("div");
  const view = createEditor(host, { value: "原文", onChange: () => {} });
  setDoc(view, "AI 改写后");
  expect(view.state.doc.toString()).toBe("AI 改写后");
  undo(view);
  expect(view.state.doc.toString()).toBe("原文");
});
```

- [ ] **Step 2: 跑测试**

Run: `npm test -- editor`
Expected: PASS（若 FAIL，说明 `setDoc` 或 createEditor 被改成了非 history 事务，需修 `editor.ts` 的 `setDoc` 确保不加 `Transaction.addToHistory.of(false)`）。

- [ ] **Step 3: （仅当上步失败）修复 setDoc**

`editor.ts` 的 `setDoc` 保持：
```typescript
export function setDoc(view: EditorView, content: string) {
  view.dispatch({ changes: { from: 0, to: view.state.doc.length, insert: content } });
}
```
不得加 `annotations: Transaction.addToHistory.of(false)`。

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "test(editor): lock in-session undo for AI writes

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 10: 更新系统提示 + 端到端 exercise

更新 `tutor-prompt.ts` 描述 7 工具与"凡写必确认"约定；`npm run tauri dev` 实测一条 set_tag 流程。

**Files:**
- Modify: `sidecar/src/tutor-prompt.ts`

- [ ] **Step 1: 更新系统提示**

把 `sidecar/src/tutor-prompt.ts` 的 `# 你拥有的工具` 段改为：
```typescript
# 你拥有的工具
- \`read_note\`：读目标笔记全文（target 缺省=当前活动笔记）。
- \`list_tags\`：列采集区已有标签与可用颜色（tag_create 选色用）。
- \`edit_note\`：唯一 str_replace，改块/插块/删块/改任务都用它。old_string 必须唯一。
- \`write_note\`：整篇覆写，仅大重构时用。
- \`set_tag\`：给采集区某块打/清标签（anchor=块首行前缀，tagId=null 清）。
- \`tag_create\`：新建标签（color 须取自 list_tags 的 freeColors）。
- \`tag_delete\`：删标签定义及其所有块标记。
所有写操作都会弹气泡让用户确认；用户可能拒绝，拒绝后不要反复重试。target 可跨文件（如在看 _inbox 时给 _tasks 加行动项）。
```
其余段落（角色、改写纪律、表达）保留。

- [ ] **Step 2: 跑全部测试**

Run: `npm test && cd sidecar && npm test && cd ../src-tauri && cargo test --lib`
Expected: 全绿。

- [ ] **Step 3: 端到端实测**

Run: `npm run tauri dev`
手动：打开一个项目空间的 `_inbox`，让 AI "给第一块打上 review 标签"。确认：①气泡出现且显示语义卡（非 marker 注释）；②_inbox 是 piece? 否——_inbox 不显示"保存快照后写入"下拉；③点"允许写入"→块上出现标签；④⌘Z 撤销恢复。再对一篇 piece 让 AI 改写，确认气泡出现"保存快照后写入"下拉，选它后版本列表多一条 `AI` 版本。

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "docs(sidecar): update tutor prompt for 7-tool + confirm flow

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec 覆盖**：
- 7 工具清单 → Task 6（+ Task 3 协议、Task 7 接线）。
- 匹配算法（edit_note raw str_replace、set_tag 归一化前缀）→ Task 2。
- 共享包抽离（方案 A）→ Task 1。
- sidecar pull 全文 → Task 7（getNoteText 往返）。
- 权限气泡往返 + 并发校验 + tool-aware 卡片 → Task 5（Rust）+ Task 8（前端）。
- 版本与写入模式（仅 piece 可选快照、散文档不给、采集区不快照）→ Task 5（write_mode 守卫 + can_snapshot）。
- 采集区会话内撤销 → Task 9（已可撤销，回归测试锁定）。
- 凡写必确认 v1 + 演进留口子 → Task 5/8（resolve_permission 的 decision/write_mode 字段已就位，会话级记忆/每工具策略为后续）。
- 错误边界（未找到/不唯一/拒绝/并发/无 UI/颜色占用）→ Task 2/5/6 的错误返回路径。
- 测试策略 → 各任务 TDD。
- tutor-prompt 更新 → Task 10。

**2. 占位符扫描**：Task 5 的 `resolve_permission` 里 `(dir, note_id, path)` 重算与 `PendingEdit` 字段补全用注释标注了"落地时补"——这是实现内细节，非占位；但为避免歧义，实现时必须把 `resolved_dir/resolved_note_id/resolved_path` 存进 `PendingEdit`，`resolve_permission` 直接读，不再调 `resolve_target`。无其他 TBD/TODO。

**3. 类型一致性**：`NoteTarget`/`EditPreview`/`EditPreviewDetail` 在 Task 3（TS）与 Task 4（Rust）字段名一致（camelCase；Rust `tag_name`→`tagName` 经 `rename_all`）。`WriteResult.denied` 在 Task 6/7 一致。`handle_apply_edit_at` 签名在 Task 5 测试与实现一致。`onApplyEditResult`/`onNoteText` 在 Task 7 测试与实现一致。

## 执行交接

计划已存 `docs/superpowers/plans/2026-07-08-ai-tools.md`。两种执行方式：

**1. Subagent-Driven（推荐）** — 每个 task 派一个新 subagent，task 间 review，迭代快。

**2. Inline Execution** — 在本会话用 executing-plans 批量执行，带 checkpoint review。

选哪种？
