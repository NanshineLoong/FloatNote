/** Re-export of the shared empty-state component (promoted to
 * `src/shared/ui/empty-state.ts`). Kept so the six note call sites keep working
 * unchanged. */
export * from "../shared/ui/empty-state";
