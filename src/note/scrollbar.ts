/** Re-export of the shared scrollbar component (promoted to
 * `src/shared/ui/scrollbar.ts`). Kept so `note-app.ts` keeps importing from
 * `./scrollbar` unchanged. */
export { initScrollbar } from "../shared/ui/scrollbar";
